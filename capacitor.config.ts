import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-github-pages-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
